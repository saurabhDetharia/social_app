import 'package:flutter/material.dart';

/// Padding --->
EdgeInsets get padding24h => const EdgeInsets.symmetric(horizontal: 24);

EdgeInsets get padding24v => const EdgeInsets.symmetric(vertical: 24);

EdgeInsets get padding12 => const EdgeInsets.symmetric(
      vertical: 12,
      horizontal: 12,
    );

EdgeInsets get padding24 => const EdgeInsets.symmetric(
      vertical: 24,
      horizontal: 24,
    );

EdgeInsets get padding12ltr => const EdgeInsets.fromLTRB(12, 12, 12, 0);

/// <---

/// Border Radius --->
BorderRadius get radius15 => BorderRadius.circular(15);

BorderRadius get radius20 => BorderRadius.circular(20);

BorderRadius get radius23 => BorderRadius.circular(23);

BorderRadius get radius35 => BorderRadius.circular(35);

BorderRadius get radius79 => BorderRadius.circular(79);

BorderRadius get radius87 => BorderRadius.circular(87);

BorderRadius get radius152 => BorderRadius.circular(152);

/// <---

/// Constant values --->
Duration get splashTimer => const Duration(seconds: 2);

/// <---
